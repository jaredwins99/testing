# Large data
data/2_palate_data_parquet_cleaned/time_differences_details.pkl
data/3_palate_data_parquet_modeling/all_locations.parquet
model_simulations/simulate_full_results.rds
model_simulations/simulate_total_results.rds

# R session history (not needed for reproducibility)
.Rhistory
.Rapp.history

# RData files (can be large and not needed)
.RData
.Ruserdata

# renv library (locally installed packages)
renv/library/
renv/staging/
renv/activate.R
renv/settings.json
renv/.gitignore

# RStudio project settings (optional, but keeps repo clean)
.Rproj.user/

renv.zip

model_fits/empirical/fit_*

fit_*
samples_*
mlflow/
fit*
samples*

mlruns/
.claude-config/
node_modules/

prereg.pdf

# --- Claude Code: settings, local config, and any generated notes -----------
# Everything Claude-related is local-only and must not be committed.
.claude/
.claude-config/
CLAUDE.md
.claude.json
.claude.json.backup
**/.claude/
**/CLAUDE.local.md
.mcp.json
