# Large data
data/2_palate_data_parquet_cleaned/time_differences_details.pkl
data/3_palate_data_parquet_modeling/all_locations.parquet
model_simulations/simulate_full_results.rds
model_simulations/simulate_total_results.rds

# R session history (not needed for reproducibility)
.Rhistory
.Rapp.history

# RData files (can be large and not needed)
.RData
.Ruserdata

# renv library (locally installed packages)
renv/library/
renv/staging/
renv/activate.R
renv/settings.json
renv/.gitignore

# RStudio project settings (optional, but keeps repo clean)
.Rproj.user/

renv.zip

model_fits/empirical/fit_*

fit_*
samples_*
mlflow/
fit*
samples*

mlruns/
.claude-config/
node_modules/

prereg.pdf

# --- Claude Code: settings, local config, and any generated notes -----------
# Everything Claude-related is local-only and must not be committed.
.claude/
.claude-config/
CLAUDE.md
.claude.json
.claude.json.backup
**/.claude/
**/CLAUDE.local.md
.mcp.json

# Claude-authored / Claude-tooling notes. Kept locally, not part of the paper.
review/code_review.md
review/review.md

# Python bytecode
__pycache__/
*.pyc

# --- Paper materials -------------------------------------------------------
# Manuscript text, prereg documents and reference PDFs of other papers.
# Kept locally; not part of the code or data pipeline.
#
# PDFs are NOT ignored as a class: everything under publication/, present/ and
# archive/forest_plots/ is generated by the pipeline and belongs in the repo.
# Only papers are ignored, and they live in the two directories named here.
paper_reference/

# Stray files pulled out of the repo root during cleanup
archive/stray_root/

# Working notes on how this repo was built, not documentation of the
# analysis. Kept locally.
BEST_PRACTICES.md
