% 2x2 framework diagram crossing Novelty x Specificity to define A1-A4.
%
%   Rows:    Novelty     (No: continued availability | Yes: new introduction)
%   Columns: Specificity (No: ABF sales              | Yes: counterpart sales)
%
%   A1 = (no  novelty, no  specificity)   A2 = (no  novelty, yes specificity)
%   A3 = (yes novelty, no  specificity)   A4 = (yes novelty, yes specificity)
%
%   Counts per cell come from the exposure_outcome_diagram structure:
%     pairings  = (# exposure item types) x (# outcomes per item)
%     estimates = (# intensities) x pairings
%
% --------- TWEAK GUIDE ---------
%   cell w / cell h ........... width / height of each cell box
%   col gap / row gap ......... spacing between header strip and grid
%   axis label sep ............ distance from grid edge to axis label
%   inner sep on cells ........ padding between cell text and border
%
% Build with:  tectonic publication/novelty_specificity_diagram.tex

\documentclass[border=10pt, tikz]{standalone}

\usepackage{tikz}
\usetikzlibrary{positioning, calc, arrows.meta, fit}

\begin{document}
\begin{tikzpicture}[
  font=\small,
  axis label/.style  = {font=\bfseries},
  level/.style       = {font=\itshape\footnotesize, text=black!65},
  cell/.style        = {draw=black!55, thick, rounded corners=2pt,
                        minimum width=82mm, minimum height=26mm,
                        align=left, inner sep=8pt, anchor=north west},
  ccell/.style       = {draw=black!50, thick, dashed, rounded corners=2pt,
                        minimum width=90mm, minimum height=22mm,
                        align=left, inner sep=7pt, anchor=north west},
  alabel/.style      = {font=\bfseries\large},
  clabel/.style      = {font=\bfseries, text=black!75},
  desc/.style        = {font=\footnotesize, text=black!75},
  countline/.style   = {font=\footnotesize, text=black!60},
  arr/.style         = {-{Latex[length=2.0mm,width=1.6mm]},
                        thick, color=black!70},
  lead/.style        = {dashed, thick, color=black!45},
]

% =============================================================
% Grid cells (2 x 2)
% =============================================================
% Cell positions are anchored at their north-west corner.
% Column x-coords:  left col @ 0,   right col @ 8.6
% Row    y-coords:  top row @ 0,    bottom row @ -3.0
\node[cell] (A1) at (0.0,  0.0) {};
\node[cell] (A2) at (8.6,  0.0) {};
\node[cell] (A3) at (0.0, -3.0) {};
\node[cell] (A4) at (8.6, -3.0) {};

% Within each cell (anchored from north-west):
%   title  at -0.20
%   desc   at -0.85
%   count1 at -1.45  (no gap between desc and counts)
%   count2 at -1.80
%   count3 at -2.15

% --- A1: no novelty, no specificity ---
\node[alabel, anchor=north west] at ($(A1.north west)+(0.25,-0.20)$) {A1};
\node[desc,   anchor=north west, text width=75mm]
  at ($(A1.north west)+(0.25,-0.85)$)
  {Analog availability (count, proportion) $\rightarrow$ ABF sales};
\node[countline, anchor=north west] at ($(A1.north west)+(0.25,-1.45)$)
  {36 exposure--outcome pairings};
\node[countline, anchor=north west] at ($(A1.north west)+(0.25,-1.80)$)
  {18 primary outcomes};
\node[countline, anchor=north west] at ($(A1.north west)+(0.25,-2.15)$)
  {36 pooled estimates};

% --- A2: no novelty, yes specificity ---
\node[alabel, anchor=north west] at ($(A2.north west)+(0.25,-0.20)$) {A2};
\node[desc,   anchor=north west, text width=75mm]
  at ($(A2.north west)+(0.25,-0.85)$)
  {Analog availability (presence, count) $\rightarrow$ counterpart sales};
\node[countline, anchor=north west] at ($(A2.north west)+(0.25,-1.45)$)
  {10 exposure--outcome pairings};
\node[countline, anchor=north west] at ($(A2.north west)+(0.25,-1.80)$)
  {10 primary outcomes};
\node[countline, anchor=north west] at ($(A2.north west)+(0.25,-2.15)$)
  {10 pooled estimates};

% --- A3: yes novelty, no specificity ---
\node[alabel, anchor=north west] at ($(A3.north west)+(0.25,-0.20)$) {A3};
\node[desc,   anchor=north west, text width=75mm]
  at ($(A3.north west)+(0.25,-0.85)$)
  {New analog introductions $\rightarrow$ ABF sales};
\node[countline, anchor=north west] at ($(A3.north west)+(0.25,-1.45)$)
  {6 exposure--outcome pairings};
\node[countline, anchor=north west] at ($(A3.north west)+(0.25,-1.80)$)
  {3 primary outcomes};
\node[countline, anchor=north west] at ($(A3.north west)+(0.25,-2.15)$)
  {12 pooled estimates};

% --- A4: yes novelty, yes specificity ---
\node[alabel, anchor=north west] at ($(A4.north west)+(0.25,-0.20)$) {A4};
\node[desc,   anchor=north west, text width=75mm]
  at ($(A4.north west)+(0.25,-0.85)$)
  {New analog introductions $\rightarrow$ counterpart sales};
\node[countline, anchor=north west] at ($(A4.north west)+(0.25,-1.45)$)
  {3 exposure--outcome pairings};
\node[countline, anchor=north west] at ($(A4.north west)+(0.25,-1.80)$)
  {3 primary outcomes};
\node[countline, anchor=north west] at ($(A4.north west)+(0.25,-2.15)$)
  {6 pooled estimates};

% =============================================================
% Column headers (Specificity axis)
% =============================================================
\node[axis label, anchor=south]
  at ($(A1.north)!0.5!(A2.north) + (0,1.10)$) {Specificity};

\node[level, anchor=south]
  at ($(A1.north) + (0,0.18)$) {No: ABF sales};
\node[level, anchor=south]
  at ($(A2.north) + (0,0.18)$) {Yes: counterpart sales};

% horizontal arrow under "Specificity" header showing direction
\draw[arr]
  ($(A1.north) + (0,0.65)$) -- ($(A2.north) + (0,0.65)$);

% =============================================================
% Row headers (Novelty axis)
% =============================================================
\node[axis label, rotate=90, anchor=south]
  at ($(A1.west)!0.5!(A3.west) + (-1.95,0)$) {Novelty};

\node[level, rotate=90, anchor=south]
  at ($(A1.west) + (-0.18,0)$) {No: continued};
\node[level, rotate=90, anchor=south]
  at ($(A3.west) + (-0.18,0)$) {Yes: new};

% vertical arrow next to "Novelty" header showing direction
\draw[arr]
  ($(A1.west) + (-1.05,0)$) -- ($(A3.west) + (-1.05,0)$);

% =============================================================
% Customer-level sensitivity (one-off, branching from A3 + A4)
% =============================================================
% Tucked tight under the bottom row, dashed border to mark it as a
% sensitivity analysis rather than a primary cell. Two straight
% vertical dashed leaders drop from A3.south and A4.south onto the
% box's top edge -- "thought emerging from both" without curves.
%
% Width is sized so the box top edge spans A3.south..A4.south, so the
% two vertical leaders land cleanly inside the corners. Description
% dropped for compactness; the bold title plus the count list is enough.
\node[ccell] (CC) at (3.9, -6.3) {};

\node[clabel, anchor=north west] at ($(CC.north west)+(0.25,-0.20)$)
  {Customer-level sensitivity};
\node[countline, anchor=north west] at ($(CC.north west)+(0.25,-0.85)$)
  {8 exposure--outcome pairings};
\node[countline, anchor=north west] at ($(CC.north west)+(0.25,-1.20)$)
  {5 primary outcomes};
\node[countline, anchor=north west] at ($(CC.north west)+(0.25,-1.55)$)
  {24 pooled estimates};

\draw[lead] (A3.south) -- (A3.south |- CC.north);
\draw[lead] (A4.south) -- (A4.south |- CC.north);

\end{tikzpicture}
\end{document}
